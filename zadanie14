14) Удаление объекта по значению свойства
function deleteObjectByPropertyValue(arr, prop, value) {
  return arr.filter(obj => obj[prop] !== value);
}

// Пример использования:
const objects = [{ id: 1 }, { id: 2 }, { id: 3 }];
console.log(deleteObjectByPropertyValue(objects, 'id', 2));
